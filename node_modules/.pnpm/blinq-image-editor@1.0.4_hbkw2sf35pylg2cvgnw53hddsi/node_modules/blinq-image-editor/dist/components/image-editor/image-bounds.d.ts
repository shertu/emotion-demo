import { IRectangle, Rectangle } from '../../business';
/**
 * Calculates the bounds of the drag.
 */
export declare function calculateImageBounds(crop: IRectangle, iW: number, iH: number, scale?: number): Rectangle;
