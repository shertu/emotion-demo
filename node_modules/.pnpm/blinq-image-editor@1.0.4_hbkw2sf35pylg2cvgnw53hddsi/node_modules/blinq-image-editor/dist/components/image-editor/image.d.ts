import React from 'react';
export interface SpringProps {
    x: number;
    y: number;
    scale: number;
    rotateZ: number;
}
declare type I = React.DetailedHTMLProps<React.ImgHTMLAttributes<HTMLImageElement>, HTMLImageElement>;
export interface ImageEditorImageProps extends SpringProps {
    src?: I['src'];
    alt?: I['src'];
    onChangeSize?: (width: number, height: number) => void;
}
/**
 * The image element which fits the image transform / bounds.
 */
export declare const ImageEditorImage: React.FC<ImageEditorImageProps>;
export default ImageEditorImage;
